server:
  port: 8888
spring:
  application:
    name: config-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig #配置文件所在仓库
          username:
          password:
          default-label: master #配置文件分支
          search-paths: respo  #配置文件所在根目录